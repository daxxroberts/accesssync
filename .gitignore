# Dependencies
node_modules/

# Environment variables — NEVER commit these
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Railway
.railway/

# Test artifacts
.test-images/

# Svelte build output — built on Railway deploy via npm run build
admin/public/dist/

# Supabase migration working artifacts (DR-047 / OB-180, 2026-05-20)
# Final products migrations/supabase-bootstrap.sql + migrations/supabase-data.sql
# are committed. The chunk + section directories are intermediate scratch files
# from the apply pipeline — regenerable from the final products.
migrations/supabase-data-chunks/
migrations/supabase-sections/
